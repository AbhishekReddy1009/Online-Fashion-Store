function add_to_cart(productName) {
    // Create a new JSON object with the product name and price
    console.log(productName)
    
    // Load the existing cart data from the cart.json file
    alert(`${productName} was added to the cart!`);
  }
  